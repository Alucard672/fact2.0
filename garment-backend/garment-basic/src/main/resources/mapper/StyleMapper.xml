<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.garment.basic.mapper.StyleMapper">

    <!-- 分页查询款式列表 -->
    <select id="selectStylePage" resultType="com.garment.basic.entity.Style">
        SELECT * FROM styles
        WHERE tenant_id = #{tenantId}
        AND deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (style_code LIKE CONCAT('%', #{keyword}, '%') 
                 OR style_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>
</mapper>