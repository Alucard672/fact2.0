<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.garment.basic.mapper.WorkshopMapper">

    <!-- 分页查询车间列表 -->
    <select id="selectWorkshopPage" resultType="com.garment.basic.entity.Workshop">
        SELECT * FROM workshops
        WHERE tenant_id = #{tenantId}
        AND deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (workshop_code LIKE CONCAT('%', #{keyword}, '%') 
                 OR workshop_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY sort_order ASC, created_at DESC
    </select>
</mapper>